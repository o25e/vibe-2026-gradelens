# Required
JWT_SECRET=replace-with-long-random-secret

# Optional AI providers
GROQ_API_KEY=
GRADING_AGENT_URL=

# Optional SQLite location.
# Vercel defaults to /tmp/gradelens.db when omitted.
# NOTE: /tmp is ephemeral and not persistent between deployments/executions.
SQLITE_DB_PATH=
